#include <bits/stdc++.h>
using namespace std;
int main()
{
    vector <int> v;
    vector <int>v(10);
    vector <int> v(10, -1);

    vector<int> vv (v); // vector- v element copy into vector- vv

    int a[N];
    vector<int> v(a, a+N); // array copy

}